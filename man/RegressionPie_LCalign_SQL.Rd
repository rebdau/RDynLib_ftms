% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RegressionPie_LCalign_SQL.R
\name{RegressionPie_LCalign_SQL}
\alias{RegressionPie_LCalign_SQL}
\title{Piecewise Robust Regression Between FTMS and QTOF Retention Times}
\usage{
RegressionPie_LCalign_SQL(LCal, startpoint = 1)
}
\arguments{
\item{LCal}{A matrix or data frame representing local alignment results,
where column 2 contains FTMS retention times and column 4 contains
QTOF retention times.}

\item{startpoint}{Numeric. Minimum FTMS retention time at which knots may be
placed. Defaults to 1.}
}
\value{
A numeric vector containing six regression parameters:
\itemize{
\item \code{exp.intercept}: intercept of the robust regression,
\item \code{exp.slope}: main slope before first knot,
\item \code{k1_best}: first knot position (0 if no knot),
\item \code{exp.t1}: slope adjustment after first knot,
\item \code{k2_best}: second knot position (0 if not used),
\item \code{exp.t2}: slope adjustment after second knot.
}
}
\description{
\code{RegressionPie_LCalign_SQL()} performs piecewise regression between FTMS and
Synapt QTOF retention times obtained after local alignment (\code{LCal}).

Because chromatograms may come from different LC systems, deviations from
linearity are common. Therefore the function evaluates:
\itemize{
\item a model without knots (simple linear regression),
\item piecewise linear models with one knot,
\item piecewise linear models with two knots.
}

For each model, the adjusted R^2 is computed, and the best model is selected.
The selected model is then refitted using robust regression (\code{rlm}) to reduce
sensitivity to outliers.

The \code{startpoint} parameter specifies the minimum FT retention time from which
knot positions can be considered, and iterates only over the unique retention
times actually present in LCal.
}
\author{
Ahlam Mentag
}
