% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ftms_all_levels.R
\name{ftms_all_levels}
\alias{ftms_all_levels}
\title{Extract full MSn fragmentation trees for features}
\usage{
ftms_all_levels(ftms, ms2)
}
\arguments{
\item{ftms}{An \linkS4class{XcmsExperiment} object containing detected
features and multi-level MSn data of a metabolomics experiment.
The data is expected to include at least MS2 and MS3 spectra and optionally
higher-order fragmentation levels (e.g. MS4 and above).}

\item{ms2}{A \linkS4class{Spectra} object containing all MS2 spectra whose
retention time and precursor m/z fall within the ranges of any
chromatographic peak of a feature in \code{ftms}, as returned for example by
\code{xcms::featureSpectra(ftms, msLevel = 2L)}.}
}
\value{
A \linkS4class{Spectra} object containing all MS2, MS3 and MS4
spectra associated with the input features. Each spectrum includes a
\code{feature_id} spectra variable identifying the originating feature.
}
\description{
This function extracts complete MSn fragmentation trees (MS2, MS3 and
MS4 spectra) associated with features from an \code{XcmsExperiment} object.
Starting from feature-associated MS2 spectra, higher-level MSn spectra
are retrieved recursively using the \code{precScanNum} spectra variable.
}
\details{
All spectra belonging to the same fragmentation tree are linked to the
originating feature through a propagated \code{feature_id}.

The function reconstructs MSn fragmentation trees by linking spectra
across MS levels within each data origin. For each distinct \code{dataOrigin}
in the input \code{ms2} object, MS2 spectra are matched to MS3 spectra using
the \code{precScanNum} and \code{acquisitionNum} spectra variables. The same
matching strategy is then applied to link MS3 spectra to MS4 spectra.

The \code{feature_id} from the MS2 spectra is propagated to all matched MS3 and
MS4 spectra, ensuring that all spectra belonging to the same fragmentation
tree can be traced back to the originating feature.

Spectra are processed independently for each \code{dataOrigin} and subsequently
combined into a single \linkS4class{Spectra} object.
}
\section{Processing steps}{

\enumerate{
\item Split MS2 spectra by \code{dataOrigin}.
\item For each subset, retrieve MS3 spectra from \code{ftms} with matching
\code{dataOrigin}.
\item Match MS2 to MS3 spectra using \code{acquisitionNum} and \code{precScanNum}
via \code{findMatches()}.
\item Propagate \code{feature_id} from MS2 to matched MS3 spectra.
\item Retrieve MS4 spectra and match them to MS3 spectra using the same
approach.
\item Propagate \code{feature_id} from MS3 to MS4 spectra.
\item Combine MS2, MS3, and MS4 spectra for each \code{dataOrigin}.
\item Concatenate results across all data origins into a single object.
}
}

\examples{
## Assume 'ftms' is an XcmsExperiment object
## Extract MS2 spectra associated with features
ms2 <- xcms::featureSpectra(ftms, msLevel = 2L)

## Retrieve full MSn trees
msn <- ftms_all_levels(ftms, ms2)

length(msn)

}
