% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_databases.R
\name{merge_databases}
\alias{merge_databases}
\title{Merge two SQLite spectral databases.}
\usage{
merge_databases(main_db, add_db, output_db)
}
\arguments{
\item{main_db}{'Character(1)' Path to the primary SQLite database.}

\item{add_db}{'Character(1)' Path to the second SQLite database that would be
added to the main database.}

\item{output_db}{Character. Path where the merged SQLite database will
be written.}
}
\value{
No value is returned. The function writes the merged database
to \code{output_db} and prints progress messages during execution.
}
\description{
Merges two SQLite databases containing mass spectrometry data into a
single output database. The function aligns tables present in both
databases, shifts identifier columns to avoid ID conflicts,
and combines the records. Tables that exist in only
one database are copied directly to the output database.

Identifier columns (\code{expid}, \code{compound_id}, \code{spectrum_id}, \code{peak_id})
are automatically shifted to ensure uniqueness across the merged data.
Retention times (\code{rtime}, \code{retention_time}) from the additional database
are converted from seconds to minutes before merging.
}
\author{
Ahlam Mentag
}
